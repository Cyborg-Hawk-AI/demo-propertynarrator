import DemoApp from "@/components/demo/DemoApp";

export const metadata = {
  title: "Demo — PropertyNarrator",
  description: "Interactive demo of PropertyNarrator deal analysis, portfolio comparison, and AI verdicts.",
};

export default function DemoPage() {
  return <DemoApp />;
}
